import { useState, useEffect } from 'react'

//           <li key={country.name.common}>

const Button = ({name,countries }) => {

    return (
        <button onClick={handleClick}>show</button>

    )
}

export default Button